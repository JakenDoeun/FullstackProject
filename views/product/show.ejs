<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= house.title %> Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="/public/css/styles.css">
</head>
<body>

  <!-- Navigation Bar -->
  <%- include('../partials/header') %>

  <!-- House Image and Details Section (Side by Side) -->
  <div class="house-details-wrapper">
    <!-- House Image Section -->
    <div class="image-container">
      <img src="<%= house.image || 'default-image.jpg' %>" alt="<%= house.title || 'No Title' %>"> <!-- House Image -->
    </div>
    <!-- House Details Section -->
    <div class="details">
      <h2><%= house.title || 'No Title' %></h2>
      <p><strong>Location:</strong> <%= house.location || 'No Location' %></p>
      <p><strong>Price:</strong> $<%= house.price || '0' %></p>
      <p><strong>Description:</strong> <%= house.description || 'No Description' %></p>
      <p><strong>Bedrooms:</strong> <%= house.bedrooms || '0' %></p>
      <p><strong>Bathrooms:</strong> <%= house.bathrooms || '0' %></p>
      <p><strong>Square Feet:</strong> <%= house.squareFeet || '0' %></p>
      <a href="/buy/<%= house.id || '#' %>" class="btn btn-outline-primary">Buy Now</a>
      <a href="/tour/<%= house.id || '#' %>" class="btn btn-outline-secondary">Schedule a Tour</a>
      <a href="/house_list" class="btn btn-outline-secondary">Back to House List</a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  <%- include('../partials/footer') %>
</body>
</html>